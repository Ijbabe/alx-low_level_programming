#include <stdio.h>

/**
 * main -print test
 * description: print printf
 * Return: Always 0 (Success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art, ");
	return (0);
}
