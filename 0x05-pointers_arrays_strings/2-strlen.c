#include "main.h"
#include <string.h>

/**
 * _strlen- funtion that prints the lenght of a string
 *
 * @s: lenght of string
 *
 * Return: 0
 */

int _strlen(char *s)
{
	char *s;
	int len;

	s = "my string";
	len = _strlen(s);
	len;
	return (0);
}
