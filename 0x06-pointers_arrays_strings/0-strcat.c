#include "main.h"
#include <string.h>

/**
 * _strcat- concatenates two strings.
 * @dest: destination
 *
 * @src: source.
 *
 * Return: destination
 */

char *_strcat(char *dest, char *src)
{
	char dest;
	char src;

	strcat(destination, source);

	return (dest);
}
